# 如何补充体裁

1. 新增 `genres/<genre-id>/`，放入 `SKILL.md` 和 `01`–`05`。
2. `SKILL.md` 的 `name` 与 `id` 必须同为 `write-<genre-id>`，kebab-case。
3. 中文名只写正文一级标题。
4. 禁止原文、作品名、作者名、作品编号、语料路径、精确测值、产品命令。
5. 成文只写规则和粗区间，并声明不是合格线。
6. 手册必须能拿去写一章：每种写法写清上场先定、步骤、纸上必须看见什么、失败后怎么改。禁止只留四行技法卡摘要。
7. 在 `genres/SKILL.md` 和 `genres/README.md` 登记该体裁。
